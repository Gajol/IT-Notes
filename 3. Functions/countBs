// 3. Functions Excercises - Bean counting
function countBs (a) {
  let countBs = 0
  for (n=0;l n < a.length; n++) {
      if (a[n] === "B") {
        countBs++
      }
  }
  return countBs
}
console.log ("3 B's I hope: " + countBs ("Big Bad Bob")
